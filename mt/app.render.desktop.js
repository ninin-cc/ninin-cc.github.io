// Desktop presentation boundary.
// Existing render() output is treated as the desktop view while mobile scenes are introduced page by page.
(function () {
  function renderRoleTradeDesktopFrame(html, context) {
    return html;
  }

  window.renderRoleTradeDesktopFrame = renderRoleTradeDesktopFrame;
})();
